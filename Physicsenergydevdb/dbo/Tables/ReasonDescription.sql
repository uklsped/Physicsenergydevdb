CREATE TABLE [dbo].[ReasonDescription] (
    [ReasDescID]        INT           IDENTITY (1, 1) NOT NULL,
    [ReasonNumber]      INT           NOT NULL,
    [ReasonDescription] NVARCHAR (50) NOT NULL
);

